# TODO — Фабрика гипотез

Журнал автономной сессии. Обновляется после каждой фазы.

## done
- [x] Проверка окружения: Python 3.13.1, node 24, git, диск ОК; данные распакованы в data/case/ (31 файл, NFC); .env заполнен, живой смоук LLM: HTTP 200, auth_style=raw, «работает», $0.026/вызов.
- [x] P0: git init, скаффолд backend/app (config, models, llm-клиент с retry/масками, FastAPI+CORS), domain_packs/flotation.yaml (синонимы меток, capture_rates, профили 8.2, онтология оборудования), pytest (5 passed), CI workflow, .env.example, .gitignore (.env, data/, *.zip).

- [x] P1: парсер xlsx (все 4 файла, нормализация «гуляющих» меток regex-ом, пустое=0/measured, #REF!=None+issue), парсер docx (эталоны: 5/6/8/8 гипотез), восстановление 7.1 (ярус 1 — инварианты I1-I6 до неподвижной точки, ярус 2 — LLM c graceful degradation). Контрольные числа раздела 3 сходятся с допуском 0.01 на реальных файлах. Пример 1: все #REF! закрыты ярусом 1 (класс +125 пуст → нули), 0 ложных срабатываний на Примере 2. ТОФ: 3 секции (породные/пирротиновые/общие), Факт/Расчёт в meta. typical_distribution пересчитан по фактическим данным (Ni/Cu раздельно). 20 тестов зелёные.

- [x] P2: диагностика R1–R5 (детерминированная, без LLM, все вычисления от тонн — восстановленные ячейки не дают NaN). Пример 2: R1/Ni (закрытый 845.73 т в +125 ✓), R2/Ni, R5в ловит аномалию Cu/пирит 56.4% в −10; R3 корректно молчит. R4 «Почему не предложено» с тоннами и причинами. КГМК: R2 (шламы 80.8% раскрытого в −10), 10 ячеек recovered_math помечены R5b. ТОФ общие: R5a ловит несходимость сумм блока «Расчёт». 27 тестов зелёные.

## blocked
- (пусто)

## deferred
- (пусто)

## заметки
- data/case и zip НЕ коммитятся (203 МБ, read-only исходники кейса).
- Тесты, зависящие от data/case, имеют skipif для CI.
