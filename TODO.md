# TODO — Фабрика гипотез

Журнал автономной сессии. Обновляется после каждой фазы.

## done
- [x] Проверка окружения: Python 3.13.1, node 24, git, диск ОК; данные распакованы в data/case/ (31 файл, NFC); .env заполнен, живой смоук LLM: HTTP 200, auth_style=raw, «работает», $0.026/вызов.
- [x] P0: git init, скаффолд backend/app (config, models, llm-клиент с retry/масками, FastAPI+CORS), domain_packs/flotation.yaml (синонимы меток, capture_rates, профили 8.2, онтология оборудования), pytest (5 passed), CI workflow, .env.example, .gitignore (.env, data/, *.zip).

- [x] P1: парсер xlsx (все 4 файла, нормализация «гуляющих» меток regex-ом, пустое=0/measured, #REF!=None+issue), парсер docx (эталоны: 5/6/8/8 гипотез), восстановление 7.1 (ярус 1 — инварианты I1-I6 до неподвижной точки, ярус 2 — LLM c graceful degradation). Контрольные числа раздела 3 сходятся с допуском 0.01 на реальных файлах. Пример 1: все #REF! закрыты ярусом 1 (класс +125 пуст → нули), 0 ложных срабатываний на Примере 2. ТОФ: 3 секции (породные/пирротиновые/общие), Факт/Расчёт в meta. typical_distribution пересчитан по фактическим данным (Ni/Cu раздельно). 20 тестов зелёные.

- [x] P2: диагностика R1–R5 (детерминированная, без LLM, все вычисления от тонн — восстановленные ячейки не дают NaN). Пример 2: R1/Ni (закрытый 845.73 т в +125 ✓), R2/Ni, R5в ловит аномалию Cu/пирит 56.4% в −10; R3 корректно молчит. R4 «Почему не предложено» с тоннами и причинами. КГМК: R2 (шламы 80.8% раскрытого в −10), 10 ячеек recovered_math помечены R5b. ТОФ общие: R5a ловит несходимость сумм блока «Расчёт». 27 тестов зелёные.

- [x] P3: KB/RAG. Нормализация грязного PDF-слоя (переносы, разреженные буквы), чанкование с перекрытием и страницами, детектор скана (5 проб, <200 симв/стр): lodeyshchikov → scan_no_text без OCR. Индекс: BM25Plus (Okapi зануляет idf на малом корпусе) с грубым стеммингом [:6] + chromadb dense (bge-m3 скачана, dim=1024) + RRF-гибрид; авто-реиндекс при смене EMBED_MODEL (8а); graceful BM25-only без модели. Глембоцкий: 304 стр → 417 чанков за 0.5 с, поиск в тему. Семантический тест: запрос про халькопирит находит ксантогенаты без лексического пересечения. 35 тестов зелёные.

- [x] Живой смоук LLM перед P4 (через backend/app/llm.py): HTTP 200, ответ «работает» за 4.4 с, usage 11+169 ток., $0.030, auth raw. Полная генерация на 120с таймауте ловила ReadTimeout (thinking-модель) → TIMEOUT_STRONG поднят до 300 с (отклонение от ТЗ, осознанное); деградация в мок при таймауте отработала штатно.
- [x] P4: генерация гипотез — промпт «главный обогатитель НИИ» (8 требований раздела 8), диагнозы+KB-чанки+онтология+стоп-лист+история → STRONG JSON; детерминированный расчёт эффекта (tonnes_max из целевых ячеек × capture_rate из yaml × цена металла); гипотезы по неизвлекаемым формам отбрасываются; неизвестное оборудование → present_on_plant=false+capex high; без ключа — мок-фикстура с заземлением цитат на реальные чанки локального индекса (verify проходит офлайн). verify.py: rapidfuzz partial_ratio>75 по тексту chunk_id, выдуманные цитаты валятся.
- [x] P5: rank.py (w1·money+w2·(1−capex)+w3·(1−risk)+w4·novelty, дефолт 0.4/0.25/0.2/0.15; novelty: эмбеддинги cos>0.8 или fuzzy>80 → prior_match, novelty=0.2; штраф ×0.75 без verified-цитат), SQLite store (проекты/отчёты/гипотезы/фидбэк/roadmap; replace не трогает принятые), API: projects/report(upload+PATCH cells→manual)/diagnostics/generate/feedback(reject+reason→стоп-лист)/kb(upload/documents/chunk/ask). Сквозной API-тест на Примере 2.

- [x] Живая генерация на Примере 2 (300с таймаут): 10 гипотез за 217 с, citation validity 90%, Ni+Cu, адекватные механизмы/планы. Пайплайн подтверждён вживую.
- [x] P6: чат 8.1 (детерминированный контекст-сборщик: отчёт+диагнозы+гипотезы кратко/упомянутые полные+топ-5 KB+веса+стоп-лист; FAST, STRONG при history>6 или «сравни»; ответ {text, references[]}, офлайн-фоллбэк с главным диагнозом), дорожная карта 8.2 (lab→ОПИ→тираж с воротами из плана, конфликт ресурса → сдвиг по score с причиной «ждёт мельницу 5-3», лаборатория cap=2, skip_lab для classification/reagent/…, PATCH с валидацией → 409), экспорт: DOCX (титул/сводка/диагнозы/топ-5/почему-не-предложено/Гант/карточки), tasks.csv (BOM для Excel), JSON. API-тест 409 через HTTP.

- [x] P7: фронтенд React+Vite+TS+Tailwind v4, 6 экранов: 1 Разбор (KPI-карточки, крупность, минералогия Ni+Cu с янтарным пунктиром восстановленных + тултип формулы + inline-правка + «Принять все восстановленные»), 2 Карта потерь (heatmap классы×формы, 🔒 неизвлекаемые, Ni/Cu, панель диагнозов с правилом по клику и CTA, аномалии R5, «Почему не предложено»), 3 Гипотезы (слайдеры весов, фильтры переделов, «без капзатрат», исключения→промпт, карточки с цитатами-чипсами→модал источника, оборудование ✓/✗, план проверки, Принять/Отклонить с причиной, бейджи «Совпадает с гипотезой экспертов»/«на восстановленных данных»), 4 Отчёт (топ-5, канбан статусов, кнопки DOCX/CSV/JSON; Гант на CSS: стадии, ромбы-ворота с критерием, линия «сегодня», сдвиг → с 409-тостом, «ждёт ресурс»), 5 База знаний (статусы, «требуется OCR», дропзона, ask с цитатами), 6 Чат-панель (чипсы-подсказки, кликабельные references → навигация/модал чанка). Vite-прокси /api. Нюанс: vite слушал только ::1 — прибит к 127.0.0.1. Сквозной флоу через прокси проверен вживую, чат ответил со ссылками [R1]+ячейки.

- [x] P8: eval/run_eval.py — 4 примера: парсинг→диагностика→генерация→LLM-судья против эталонных docx; метрики coverage/novel/citation_validity/parse_ok; --mock (fuzzy-судья, фикстура) и --no-report для pytest. Два живых прогона: №1 coverage 34%, cit_validity 97% → анализ промахов → intervention_menu в yaml + правка промпта → №2 coverage 55% (Пример 2 — 83%), cit_validity 93%, parse_ok 4/4, novel 23. Цель 60% не добита: остаток — экспертные ходы вне данных и литературы (Finfix 300, шары 5 класса) — закрывается пополнением KB. +7 тестов покрытия (пропагация восстановления, ТОФ-детали, границы Ганта). Итого 68 тестов.

## blocked
- (пусто)

## deferred
- (пусто)

## заметки
- data/case и zip НЕ коммитятся (203 МБ, read-only исходники кейса).
- Тесты, зависящие от data/case, имеют skipif для CI.
