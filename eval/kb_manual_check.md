# Ручная валидация kb_yandex_v2 глазами скептика

Боевой сценарий: найдётся ли в top-5 материал для цитат (≤40 слов) под реальные
экспертные гипотезы. Инструмент: `scripts/kb_query.py "запрос" [N]`.

## ШАГ 0 — проверка асимметрии моделей

Код проверен, путаницы doc/query НЕТ. `kb/embedder_yandex.py`:

```python
def embed_documents(self, texts: list[str]) -> list[list[float]]:
    # батч не поддерживается API — до 8 параллельных одиночных запросов
    with ThreadPoolExecutor(max_workers=MAX_PARALLEL) as pool:
        return list(pool.map(lambda t: self._post_one(self.doc_model, t), texts))

def embed_query(self, text: str) -> list[float]:
    return self._post_one(self.query_model, text)      # <- строки 100-101
```

Запросы идут через `text-search-query`, документы — через `text-search-doc`;
поисковые скрипты (`check_kb_yandex.py`, `kb_query.py`) зовут именно `embed_query`.

## ШАГИ 2–3 — прогон и оценка

| # | Запрос | Цитируемость | Адресность | Мусор в top-5 |
|---|---|---|---|---|
| 1 | замена насадок гидроциклонов крупность слива | **FAIL** — про насадки в top-5 ничего; лучшая близкая цитата (Пелевин, с. 7: «Граница разделения в гидроциклоне (0,042 мм) ниже, чем в грохоте (0,078 мм)…») обосновывает грохоты, а не замену насадок | **PASS** (Пелевин-2022 на поз. 2 и 4) | `geokniga-metallurgiya…:0091` (дренажные сетки пачука) и `:0058` (выщелачивание) — офф-топик из чужого домена; `2022_pelevin…:0004` — подпись к рисунку с англ. дублем |
| 2 | износ футеровки влияние на измельчение | **PASS** — Болобов, с. 6: «Указанное упрочнение стали способствует увеличению ее стойкости к истиранию мягкими породами примерно в 2 раза (с 0,0054 до 0,0028 мм/ч)» (18 слов) | **PASS** (Болобов-2014 — топ-1 и топ-2) | хвост чанка `2014_bolobov…:0005` захватил начало «СПИСОК ЛИТЕРАТУРЫ»; позиции 3–5 — офф-топик про шламы |
| 3 | депрессия пирротина реагент извлечение пентландита | **PASS** — патент RU 2108168: «предлагаемый способ позволяет значительно усилить депрессирующее воздействие солей дитиокарбаминовой кислоты на флотацию пирротина, сохранив при этом высокую избирательность процесса по отношению к пентландиту» (24 слова) | **PASS** (патент RU в топе; Манцевича-2008 нет, но критерий «или патенты RU» выполнен) | `2108168…:0000` — шапка патента (заголовок + список из 18 авторов); `:0014` — юридическая формула новизны, для цитат бесполезна |
| 4 | потери никеля со шламами тонкие классы флотация | **FAIL** — цитаты, связывающей потери Ni именно со шламами/тонкими классами, в top-5 нет: патент даёт потери Ni от времени флотации, Алгебраистова — «С хвостами теряется 61,3 % никеля» без привязки к крупности | **FAIL** — ожидались Усманова/Лавриненко, их нет вообще | `2013_algebraistova…:0007` — табличный чанк (шапка таблицы + реагентный режим) |
| 5 | тонкое грохочение вместо гидроциклонов циркулирующая нагрузка | **PASS** — Derrick: «использование тонкого грохочения, классифицирующего частицы только по геометрическому размеру, может давать значительные преимущества как в смысле производительности мельницы, так и снижая удельное энергопотребление на тонну измельчаемой продукции» (27 слов) | **PASS** (Derrick — весь top-5) | `2006_stroyka…:0007` — перечень подписей к рисункам («Рис. 3. Схема… Рис. 4. Грохот Stack Sizer») |
| 6 | время контрольной флотации извлечение | **FAIL** — тематически близкие чанки есть (кинетика, §7), но вырезаемой цитаты «время ↑ → извлечение ↑» нет: формула кинетики ε(K,t) в чанке `…:0161` разрушена OCR («е = С f { \ - - < r Kt)») и в карточку не годится | **PASS** (Глембоцкий 4 из 5, Олиферович на 5-й позиции) | `geokniga-flotacionnye…:0194` — типографский колонтитул «8 Заказ № 533» внутри чанка; `:0219` — OCR-каша в подписи схемы («Лцгпанце Н еправильно Хвосты…») |

**Итог по цитируемости: 3/6.** Адресность: 5/6. Провалы — лексически-узкие темы
(насадки, шламовые потери Ni, время контрольной флотации), где dense-256 тянет
«общесмысловых» соседей вместо точного термина.

## ШАГ 4 — тест на перепутанность моделей

Запрос №3 развёрнутой фразой («какими реагентами достигается депрессия пирротина
при флотации медно-никелевых руд и как это влияет на извлечение пентландита»):
top-1 dist 0.392 против 0.489 у короткой версии, состав топа тот же (патент
RU 2108168 + Глембоцкий про пирротин). Улучшение умеренное и ожидаемое (больше
контекста в запросе), НЕ систематически-драматическое → симптома путаницы
doc/query нет, что согласуется с проверкой кода в ШАГЕ 0.

## Предложение фильтра служебных чанков (НЕ применено — только патч-идея)

```python
# kb/chunking.py — кандидат: отсев/усечение служебных чанков перед выдачей
SERVICE_MARKERS = re.compile(
    r"^\s*(СПИСОК ЛИТЕРАТУРЫ|БИБЛИОГРАФИЧЕСКИЙ СПИСОК|СОДЕРЖАНИЕ|ОГЛАВЛЕНИЕ|REFERENCES)",
    re.IGNORECASE | re.MULTILINE)

def is_service_chunk(text: str) -> bool:
    if SERVICE_MARKERS.search(text[:300]):
        return True                                   # титулы разделов-справочников
    digits = sum(c.isdigit() for c in text) / max(len(text), 1)
    if digits > 0.22:
        return True                                   # таблицы/шапки таблиц
    lines = [l for l in text.splitlines() if l.strip()]
    short = sum(1 for l in lines if len(l.strip()) < 25)
    if lines and short / len(lines) > 0.6:
        return True                                   # оглавления, столбцы таблиц
    captions = len(re.findall(r"\b(Рис\.|Табл(?:ица)?\.?|Fig\.)", text))
    return captions * 1000 / max(len(text), 1) > 3    # сплошные подписи к рисункам

# + усечение хвоста: text = SERVICE_MARKERS.split(text)[0] — чтобы «СПИСОК ЛИТЕРАТУРЫ»
# не съедал последний содержательный чанк статьи (кейс bolobov:0005)
```

Оценка эффекта: убрал бы 5 из 8 замеченных мусорных попаданий (шапка патента —
отдельный случай: у патентных txt первую сотню строк с перечнем авторов лучше
резать на этапе fetch).

## Вердикт

Индекс пригоден как источник цитат для генератора **с оговоркой**: чистый dense-256
уверенно закрывает «широкие» темы (футеровка, депрессия пирротина, грохочение —
цитаты вырезаются дословно и адресно), но проваливает лексически-точные запросы:
«насадки гидроциклонов», «потери со шламами», «время контрольной флотации» дают
общесмысловых соседей вместо точного термина. Именно этот класс промахов закрывает
BM25 в гибридном поиске основного приложения — термины «насадка», «шламы», «контрольная
флотация» в корпусе лексически присутствуют (Пелевин, Глембоцкий §7 «Кинетика», Усманова),
BM25 их найдёт по точному совпадению, а RRF смешает с dense-выдачей. Останутся слабыми
даже в гибриде: (1) «замена насадок ГЦ» — материала о влиянии диаметра насадки на слив
в корпусе, похоже, нет вовсе (кандидат на пополнение корпуса методичкой по гидроциклонам);
(2) OCR-битые формулы кинетики у Глембоцкого — цитата есть по смыслу, но текст нечитаем.
Служебный мусор (колонтитулы, подписи рисунков, шапки таблиц, списки литературы) занимает
~15% топов и лечится фильтром выше без переиндексации всего — только пересборкой чанков.
